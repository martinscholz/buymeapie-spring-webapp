services:
  buymeapie-webapp:
    build: .
    ports:
      - "8080:8080"
    environment:
      BUYMEAPIE_USERNAME: ${BUYMEAPIE_USERNAME}
      BUYMEAPIE_PIN: ${BUYMEAPIE_PIN}
      BUYMEAPIE_BASE_URL: ${BUYMEAPIE_BASE_URL:-https://api.buymeapie.com}
